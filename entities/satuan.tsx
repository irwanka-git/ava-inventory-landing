
export interface EntitySatuan {
    index?: number;   // corresponds to int32 in Go
    satuan_id: number;   // corresponds to int32 in Go
    satuan: string;      // corresponds to string in Go
    uuid: string;        // corresponds to string in Go
}